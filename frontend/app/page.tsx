export default function OverviewPage() {
  return <section className="max-w-3xl space-y-6"><div><p className="text-sm font-medium text-accent">Identity and shell · F1</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Development identity is verified before entering the workspace.</h1><p className="mt-3 text-muted">The shell shows the effective user, tenant, and role returned by <code>/me</code>. This is a development connection—not a production login or tenant switcher.</p></div><div className="rounded-lg border border-line bg-panel p-5 shadow-panel"><h2 className="font-semibold">Development connection</h2><p className="mt-2 text-sm text-muted">Requests flow through a same-origin Next.js proxy. Configure its backend origin and development-only identity in <code>.env.local</code>; no identity headers are exposed to browser code.</p></div><div className="rounded-lg border border-line bg-panel p-5 shadow-panel"><h2 className="font-semibold">What comes next</h2><p className="mt-2 text-sm text-muted">Documents, queries, and traces remain marked as planned until their feature slices are implemented.</p></div></section>;
}
