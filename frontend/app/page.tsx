export default function OverviewPage() {
  return <section className="max-w-3xl space-y-6"><div><p className="text-sm font-medium text-accent">Foundation · F0</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">A trustworthy frontend boundary is ready.</h1><p className="mt-3 text-muted">This shell is intentionally capability-aware. It only connects to health, readiness, and development identity endpoints today.</p></div><div className="rounded-lg border border-line bg-panel p-5 shadow-panel"><h2 className="font-semibold">Development connection</h2><p className="mt-2 text-sm text-muted">Requests flow through a same-origin Next.js proxy. Configure its backend origin and development-only identity in <code>.env.local</code>; no identity headers are exposed to browser code.</p></div><div className="rounded-lg border border-line bg-panel p-5 shadow-panel"><h2 className="font-semibold">What comes next</h2><p className="mt-2 text-sm text-muted">F1 can bootstrap <code>/me</code>. Documents, queries, and traces remain marked as planned until their feature slices are implemented.</p></div></section>;
}
